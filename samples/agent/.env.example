# Copy to .env and fill in. ConfigModule.forRoot() reads .env at bootstrap.

# Any provider key initChatModel understands. The sample asks for an OpenAI model.
OPENAI_API_KEY=

# A reachable MongoDB. The agent only issues read commands against it.
MONGO_URI=mongodb://localhost:27017
MONGO_DB=sample
